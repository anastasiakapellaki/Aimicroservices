FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn httpx

COPY provider/ ./provider/

EXPOSE 8000

CMD ["uvicorn", "provider.main:app", "--host", "0.0.0.0", "--port", "8000"]
