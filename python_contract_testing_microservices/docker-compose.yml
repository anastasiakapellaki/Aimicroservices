services:

  provider:
    build:
      context: .
      dockerfile: Dockerfile.provider
    container_name: invoice-provider
    ports:
      - "8000:8000"     # REST API → http://localhost:8000/docs
